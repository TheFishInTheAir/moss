#include <stdio.h>
#include "moss.h"


#include "esp_attr.h"
#include "esp_err.h"

#include "esp_private/esp_clk.h"

#include <esp_system.h>
#include <esp_log.h>
#include <esp_cpu.h>
#include "esp_timer.h"

static const char* TAG = "STARTUP";

void start_cpu0()
{
    
    ESP_EARLY_LOGI(TAG, "Overriding Default Startup");
    ESP_EARLY_LOGI(TAG, "Pro cpu start use code");
    int cpu_freq = esp_clk_cpu_freq();

    char testing[256];
    sprintf(testing, "CPU FREQ: %d", cpu_freq);
    ESP_EARLY_LOGI(TAG, testing);
    
    
  
    func();
    
}

void func(void)
{
    printf("Do the thing!!\n");
}

void lol(void)
{
    printf("lol\n");
}
